import type { Metadata } from "next";
import { Formula } from "../../components/Formula";
import { RightQaIndex } from "../../components/RightQaIndex";
import { TrainingSidebar } from "../../components/TrainingSidebar";

const title = "什么是 Agent Memory？";
const description = "系统讲解 Agent Memory：短期与长期、语义/情景/程序性记忆、写入、结构化存储、检索、上下文注入、更新、冲突、遗忘、安全与评测。";
export const metadata: Metadata = { title, description, openGraph: { title, description, type: "article", images: [] }, twitter: { card: "summary", title, description, images: [] } };

const chapters = [
  ["01", "为什么需要 Memory"], ["02", "它不等于什么"], ["03", "短期、长期与框架映射"],
  ["04", "语义、情景与程序性"], ["05", "完整生命周期"], ["06", "写入与准入"],
  ["07", "记录结构与存储选型"], ["08", "检索与 Token Budget"], ["09", "更新、合并与遗忘"],
  ["10", "注入与使用"], ["11", "多 Agent 作用域"], ["12", "安全与隐私"],
  ["13", "框架落地、数据流与评测"], ["14", "关联 QA"],
];

const qaQuestions = [
  "Memory 与上下文窗口有什么区别？", "保存完整聊天历史不就够了吗？", "Memory 与 RAG 有什么区别？",
  "Vector Database 就等于 Memory 吗？", "KV Cache 是 Agent Memory 吗？", "模型参数里的知识算 Memory 吗？",
  "每条用户消息都应该写入吗？", "什么时候应该更新而不是新增？", "为什么检索最相似的内容仍可能出错？",
  "怎样处理互相矛盾的记忆？", "Agent 应该主动遗忘吗？", "多 Agent 能共享全部 Memory 吗？",
  "怎样评测 Memory 系统？", "Memory 会不会让 Agent 更容易产生幻觉？",
  "框架里的 Session 就等于长期 Memory 吗？", "LangGraph Checkpointer 与 Store 有什么区别？",
];

export default function MemoryPage() {
  return <div className="site-shell">
    <header className="topbar"><a className="brand" href="/" aria-label="模见首页"><span className="brand-mark">模</span><span>模见 <small>Model Atlas</small></span></a><nav className="topnav"><a className="active" href="/">知识库</a><a href="#chapter-1">Memory 导读</a><a href="#qa">QA 索引</a></nav><div className="top-actions"><a className="search-hint" href="#qa">⌘ K&nbsp;&nbsp;QA 速查</a><span className="edition">2026.08</span></div></header>
    <TrainingSidebar selected="memory" />
    <main className="article rms-article">
      <section className="hero rms-hero" id="top"><div className="breadcrumbs"><a href="/">知识库</a><span>/</span><a href="/agents/agent">Agent 与应用</a><span>/</span>Memory</div><div className="question-label"><span>AGENT SYSTEMS · 002</span></div><p className="original-question">“上下文窗口有限，完整聊天历史越来越长，用户偏好还会变化。Agent 究竟应该记住什么、什么时候想起来、旧信息冲突时又该相信谁？”</p><div className="eyebrow"><span></span>FROM STORED TEXT TO MANAGED EXPERIENCE</div><h1>什么是 Agent Memory？</h1><p className="dek">Agent Memory 不是“把历史消息全部塞回 Prompt”，而是一条受治理的信息生命周期：从交互和 Tool Results 中选择值得保留的内容，形成带身份、时间、来源和置信度的记录；在未来任务中按相关性与权限召回，压入有限 Context，并在新证据出现时更新、合并、失效或删除。</p><div className="hero-meta"><span>更新于 2026.08.25</span><i></i><span>72 分钟阅读</span><i></i><span>存储 + 数据流 + 框架实现</span></div><aside className="answer-first"><span>先记住一条主线</span><p><b>Memory 的价值不在“存得多”，而在“未来需要时取对、取少、取可信，并且知道什么时候不该再相信”。</b>一个只有 Vector Database 的系统只有存储与搜索能力；只有把写入、检索、注入、更新和治理接入 <a className="term-link" href="/agents/agent">Agent</a> 决策循环，才形成完整 Memory。</p></aside></section>

      <section className="prose-section" id="chapter-1"><div className="prose-heading"><span>01</span><div><p>WHY AN AGENT NEEDS EXTERNAL MEMORY</p><h2>模型已经有上下文和参数，为什么还需要 Memory？</h2></div></div><p><a className="term-link" href="/agents/agent">Agent</a> 的任务可能跨越很多步骤、多个会话和真实世界状态变化。模型参数更新慢且不适合保存某个用户刚刚产生的私有事实；Context Window 虽然能承载当前信息，却有长度、成本和注意力稀释问题。因此需要一个模型外、可读写、可治理的持久层。</p><Formula label="Memory-augmented Agent Policy" className="compact-latex" tex={String.raw`a_t\sim\pi_\theta\!\left(\cdot\mid x,s_t,o_t,\underbrace{R(q_t,\mathcal M)}_{\text{retrieved memories}}\right)`} /><p><Formula inline tex={String.raw`\mathcal M`} /> 是外部 Memory Store，<Formula inline tex={String.raw`R`} /> 是检索与选择函数。模型不会在每一步看到整个 Store，只看到当前 Query 下被选中的少量记录。</p><div className="reason-list compact-reasons"><article><span>01</span><div><h3>跨会话连续性</h3><p>记住用户偏好、未完成任务和已确认事实。</p></div></article><article><span>02</span><div><h3>经验复用</h3><p>召回过去成功或失败的任务轨迹，减少重复试错。</p></div></article><article><span>03</span><div><h3>上下文控制</h3><p>只注入当前真正相关的信息，降低 Token、延迟和干扰。</p></div></article><article><span>04</span><div><h3>可更新知识</h3><p>新事实无需重新训练模型即可写入、修正或撤销。</p></div></article></div></section>

      <section className="prose-section" id="chapter-2"><div className="prose-heading"><span>02</span><div><p>MEMORY IS NOT A SYNONYM FOR STORAGE</p><h2>Agent Memory 不等于哪些相近概念？</h2></div></div><div className="norm-table-wrap"><table className="norm-table"><thead><tr><th>对象</th><th>保存在哪里</th><th>主要作用</th><th>为什么不等于 Agent Memory</th></tr></thead><tbody><tr><td>Context Window</td><td>本次模型输入</td><td>让模型当前可见</td><td>调用结束后不会自动持久化，也不负责未来检索</td></tr><tr><td>Chat History</td><td>消息日志</td><td>重放对话</td><td>是原始材料，不包含选择、合并和遗忘策略</td></tr><tr><td>RAG Knowledge Base</td><td>外部文档库</td><td>检索相对稳定的知识</td><td>通常面向公共文档，而 Memory 还要处理个人经历和持续更新</td></tr><tr><td>Vector Database</td><td>向量索引与元数据</td><td>相似度搜索</td><td>只是可能的存储组件，不决定何时写、如何冲突和怎样使用</td></tr><tr><td>KV Cache</td><td>推理设备内存</td><td>复用当前序列 Attention 的 K/V</td><td>是计算缓存，不是可治理的跨任务记忆</td></tr><tr><td>Model Weights</td><td>神经网络参数</td><td>承载训练所得能力和知识</td><td>不适合按用户实时写入、审计、删除和精确溯源</td></tr><tr><td>Trace / Audit Log</td><td>不可变事件日志</td><td>调试与审计</td><td>记录发生过什么，不代表应把全部内容召回给模型</td></tr></tbody></table></div><aside className="boundary-box"><b>RAG 与 Memory 可以共享基础设施</b><p>二者都可能使用 Embeddings、Vector Search、Metadata Filter 和 Reranker。区别更多在数据语义与生命周期：RAG 常检索外部知识，Memory 常从 <a className="term-link" href="/agents/agent">Agent</a> 自己的交互中持续写入、更新和个性化。</p></aside></section>

      <section className="prose-section" id="chapter-3"><div className="prose-heading"><span>03</span><div><p>SCOPE BEFORE TYPE</p><h2>短期、长期 Memory 在工程框架里分别对应什么？</h2></div></div><p>最先应按<b>召回作用域</b>区分，而不是按数据库产品区分：</p><div className="compare-columns"><article><span>SHORT-TERM · THREAD SCOPED</span><h3>当前 Thread 的工作状态</h3><p>消息历史、当前计划、Tool Results、待办步骤、临时文件引用。通常由 Checkpoint 或 Session 持久化，以便中断恢复。</p></article><article className="accent-card"><span>LONG-TERM · CROSS THREAD</span><h3>跨会话可复用的信息</h3><p>用户偏好、稳定事实、过去经历、成功案例和长期规则。通常存入带 Namespace 的独立 Store。</p></article></div><div className="norm-table-wrap"><table className="norm-table"><thead><tr><th>框架概念</th><th>工程语义</th><th>默认更接近哪一层</th><th>需要注意</th></tr></thead><tbody><tr><td>LangGraph Checkpointer</td><td>按 Thread 保存 Graph State 和消息，支持暂停与恢复</td><td>Short-term / Thread State</td><td>能持久化不等于跨 Thread 长期记忆</td></tr><tr><td>LangGraph Store</td><td>按自定义 Namespace 保存并检索记录</td><td>Long-term Memory</td><td>仍需自行设计写入、冲突和 ACL</td></tr><tr><td>LangChain Agent State</td><td>当前 Agent Loop 的 Messages 与自定义字段</td><td>Short-term / Working State</td><td>通常由底层 LangGraph Runtime 管理</td></tr><tr><td>OpenAI Agents SDK Session</td><td>自动取回并保存同一会话的输入历史</td><td>Conversation History</td><td>不是自动抽取用户画像的长期 Memory</td></tr><tr><td>Provider Conversation ID</td><td>服务端串联多轮模型上下文</td><td>Conversation Continuation</td><td>生命周期、可编辑性与跨 Provider 能力受 API 约束</td></tr></tbody></table></div><Formula label="召回作用域" className="compact-latex" tex={String.raw`\mathcal M=\mathcal M_{\mathrm{thread}}\cup\mathcal M_{\mathrm{user}}\cup\mathcal M_{\mathrm{agent}}\cup\mathcal M_{\mathrm{shared}}`} /><div className="norm-table-wrap"><table className="norm-table"><thead><tr><th>Scope</th><th>谁能读</th><th>例子</th></tr></thead><tbody><tr><td>Thread</td><td>当前会话或任务</td><td>本轮已查询过的订单、待执行计划</td></tr><tr><td>User</td><td>同一用户的授权会话</td><td>语言、通知方式、长期偏好</td></tr><tr><td>Agent</td><td>某个角色或版本</td><td>该 Agent 的案例与反思</td></tr><tr><td>Shared / Organization</td><td>授权团队或多个 Agents</td><td>经过审核的 SOP、公共经验</td></tr></tbody></table></div><p>短期和长期并不等于“内存数据库”和“磁盘数据库”。数据库是否落盘描述的是 Durability；Thread、User、Organization 描述的是 Scope。一个可跨进程恢复的 Checkpoint 仍然可能只属于单个 Thread。</p></section>

      <section className="prose-section" id="chapter-4"><div className="prose-heading"><span>04</span><div><p>WHAT KIND OF THING IS REMEMBERED</p><h2>语义、情景与程序性 Memory 分别保存什么？</h2></div></div><div className="norm-table-wrap"><table className="norm-table"><thead><tr><th>类型</th><th>保存内容</th><th>Agent 示例</th><th>典型表示</th></tr></thead><tbody><tr><td>Semantic</td><td>事实与概念</td><td>用户偏好中文；项目使用 PostgreSQL</td><td>Profile、Facts、实体关系</td></tr><tr><td>Episodic</td><td>发生过的经历</td><td>上次退款失败是因为缺少审批；某类 Bug 怎样修复</td><td>事件、轨迹摘要、成功/失败案例</td></tr><tr><td>Procedural</td><td>怎样行动的规则</td><td>退款前先核验身份；发布前必须跑测试</td><td>Instructions、SOP、Policy、Prompt Rules</td></tr></tbody></table></div><aside className="text-note"><b>Semantic Memory 不等于 Semantic Search</b><p>前者描述保存的是“事实知识”；后者描述用 Embedding 相似度检索。Semantic Memory 可以用结构化 SQL 精确读取，Episodic Memory 也可以用 Semantic Search 召回。</p></aside><p>任务 State 常被单独管理，因为“当前已执行到第 3 步”需要强一致性，不能依赖模糊检索。程序性 Memory 若会改变行为和权限，也应经过版本控制与审核，而不是让模型静默重写 System Prompt。</p></section>

      <section className="prose-section" id="chapter-5"><div className="prose-heading"><span>05</span><div><p>THE MEMORY LIFECYCLE</p><h2>从一次交互到未来召回，中间发生什么？</h2></div></div><div className="formula-sequence"><div><span>01 · OBSERVE</span><Formula className="sequence-latex" tex={String.raw`e_t=(\text{message, action, result})`} /></div><div><span>02 · ADMIT</span><Formula className="sequence-latex" tex={String.raw`w_t\in\{0,1\}`} /></div><div><span>03 · EXTRACT</span><Formula className="sequence-latex" tex={String.raw`e_t\rightarrow m_i`} /></div><div><span>04 · ORGANIZE</span><Formula className="sequence-latex" tex={String.raw`\text{scope + type + links}`} /></div><div><span>05 · RETRIEVE</span><Formula className="sequence-latex" tex={String.raw`R(q,\mathcal M)\rightarrow\mathcal M_q`} /></div><div><span>06 · INJECT</span><Formula className="sequence-latex" tex={String.raw`\mathcal M_q\rightarrow\text{context}`} /></div><div><span>07 · EVOLVE</span><Formula className="sequence-latex" tex={String.raw`\text{update | merge | expire | delete}`} /></div></div><p>这条链分为两条路径：Write Path 决定哪些观察进入 Store；Read Path 决定哪些已有记录进入当前 Context。两者任一失控都会出问题：乱写导致污染，乱读导致错误或无关信息干扰决策。</p><Formula label="Memory 系统是写路径与读路径的组合" tex={String.raw`\operatorname{MemorySystem}=\operatorname{WritePath}\circ\operatorname{ReadPath}\circ\operatorname{Governance}`} /></section>

      <section className="prose-section" id="chapter-6"><div className="prose-heading"><span>06</span><div><p>DO NOT STORE EVERYTHING</p><h2>什么信息值得写入 Memory？</h2></div></div><p>最简单但最危险的策略是把每条消息原样写入。更可靠的系统先做 Admission：评估未来复用价值、稳定性、新颖性、来源可信度、敏感性和写入成本。</p><Formula label="Memory Write Gate" className="compact-latex" tex={String.raw`W(e)=\mathbf 1\!\left[\alpha U(e)+\beta N(e)+\gamma I(e)+\delta C(e)-\eta P(e)-\zeta D(e)>\tau\right]`} /><div className="norm-table-wrap"><table className="norm-table"><thead><tr><th>项</th><th>含义</th><th>问题</th></tr></thead><tbody><tr><td><Formula inline tex={String.raw`U`} /></td><td>Future utility</td><td>未来任务可能再次需要吗？</td></tr><tr><td><Formula inline tex={String.raw`N`} /></td><td>Novelty</td><td>Store 中是否已经存在？</td></tr><tr><td><Formula inline tex={String.raw`I`} /></td><td>Importance</td><td>忘记它会造成多大损失？</td></tr><tr><td><Formula inline tex={String.raw`C`} /></td><td>Confidence</td><td>来源是否明确、可验证？</td></tr><tr><td><Formula inline tex={String.raw`P`} /></td><td>Privacy risk</td><td>是否敏感、是否获得授权？</td></tr><tr><td><Formula inline tex={String.raw`D`} /></td><td>Duplication / instability</td><td>是否重复、临时或容易过期？</td></tr></tbody></table></div><p>实际系统不必使用这条精确线性公式；它是设计检查表。Admission 可以由规则、Extractor Model、用户显式指令或后台 Consolidation Job 共同完成。</p><aside className="boundary-box"><b>用户说过，不等于事实已被确认</b><p>“我的会员等级是钻石”可以被保存为 <Formula inline tex={String.raw`\text{user\_claim}`} />，但只有会员系统查询结果才能升级为 <Formula inline tex={String.raw`\text{verified\_fact}`} />。Memory 必须保存来源与证据状态，不能把自然语言陈述自动变成权威事实。</p></aside></section>

      <section className="prose-section" id="chapter-7"><div className="prose-heading"><span>07</span><div><p>REPRESENTATION AND STORAGE</p><h2>一条 Memory 应该长什么样，又应该存在哪里？</h2></div></div><Formula label="Memory Record 的最小结构" className="compact-latex" tex={String.raw`m_i=(\text{id},\text{namespace},\text{type},\text{content},\text{source},\text{valid time},\text{confidence},\text{version},\text{ACL},\text{embedding?})`} /><p><b>Embedding 是可选索引，不是事实本体。</b>权威字段、来源、版本和权限应保存在可查询的 Record 中；向量可以重建，原始证据和版本关系不能只塞在向量 Metadata 里。</p><div className="norm-table-wrap"><table className="norm-table"><thead><tr><th>表示方式</th><th>优点</th><th>局限</th><th>适合内容</th></tr></thead><tbody><tr><td>Raw Event Log</td><td>证据完整、可回放</td><td>冗长、检索噪声大</td><td>审计与重建</td></tr><tr><td>Running Summary</td><td>节省 Context</td><td>摘要会遗漏细节并累积误差</td><td>长 Thread 压缩</td></tr><tr><td>Structured Profile</td><td>字段明确、更新可控</td><td>Schema 容量有限</td><td>用户偏好与稳定属性</td></tr><tr><td>Fact Collection</td><td>可持续扩展与检索</td><td>需要去重和冲突处理</td><td>大量离散事实</td></tr><tr><td>Episodes / Cases</td><td>保留情境、动作和结果</td><td>内容长、召回成本高</td><td>经验复用与 Few-shot</td></tr><tr><td>Graph</td><td>显式关系与演化</td><td>抽取和维护复杂</td><td>实体关系、时间线与依赖</td></tr></tbody></table></div><div className="engineering-stack"><article><span>SOURCE OF TRUTH</span><b>PostgreSQL / Document Store</b><small>Records、版本、ACL、有效时间、实体字段</small></article><article><span>SEMANTIC INDEX</span><b>pgvector / Vector Database</b><small>Embedding Candidate Search；不是唯一事实源</small></article><article><span>HOT STATE</span><b>Redis / Checkpoint Store</b><small>短期 Thread State、Locks、TTL 与恢复点</small></article><article><span>RAW EVIDENCE</span><b>Event Log / Object Storage</b><small>原始消息、Tool Results、附件与审计回放</small></article><article><span>BACKGROUND JOBS</span><b>Queue + Workers</b><small>Extraction、Embedding、Dedup、Consolidation、Deletion</small></article></div><p>小规模系统完全可以从“一张 PostgreSQL Memory 表 + JSONB + pgvector + 一张不可变 Event 表”开始；只有出现明确的吞吐、图关系或多模态对象需求时，再拆成专用组件。生产系统常同时保留不可变 Raw Events 和可变的 Derived Memories：摘要或 Fact 被纠正时，不应覆盖原始证据。</p></section>

      <section className="prose-section" id="chapter-8"><div className="prose-heading"><span>08</span><div><p>RETRIEVAL IS A DECISION UNDER BUDGET</p><h2>一次模型调用之前，Memory 读路径实际做了什么？</h2></div></div><p>Memory 通常在 <a className="term-link" href="/agents/agent">Agent</a> 的 Context Builder 中读取，而不是让模型直接扫描数据库。只按 Embedding 相似度排序并不够：相关但过期、来源不可信、权限不匹配或重复的记录都可能伤害当前决策。</p><Formula label="Memory Retrieval Score" className="compact-latex" tex={String.raw`S_i=\alpha\operatorname{sim}(q,e_i)+\beta\operatorname{recency}_i+\gamma\operatorname{importance}_i+\delta\operatorname{confidence}_i+\kappa\operatorname{taskfit}_i-\eta\operatorname{redundancy}_i`} /><div className="agent-runtime-flow" role="img" aria-label="Memory 从请求解析到过滤、混合检索、重排和注入模型上下文的工程流程"><article><span>01 · RESOLVE</span><b>Identity & Scope</b><small>tenant_id、user_id、thread_id、purpose</small></article><article><span>02 · QUERY</span><b>Query Builder</b><small>Goal、entities、current State</small></article><article><span>03 · FILTER</span><b>Hard Constraints</b><small>ACL、valid time、type、deleted=false</small></article><article><span>04 · SEARCH</span><b>Hybrid Retrieval</b><small>SQL/keyword + vector + entity keys</small></article><article><span>05 · RERANK</span><b>Task-aware Ranker</b><small>相关性、权威性、时效与置信度</small></article><article><span>06 · RESOLVE</span><b>Conflict & Dedup</b><small>同字段版本、重复、互斥事实</small></article><article><span>07 · BUDGET</span><b>Context Pack</b><small>Top records within token budget</small></article><article><span>08 · INJECT</span><b>Typed Context</b><small>标注来源、时间与不可信数据边界</small></article></div><p>硬过滤必须发生在相似度排序之前，尤其是 Tenant/User Scope、ACL、时间有效区间、Memory Type、实体 ID 和删除状态。软分数永远不能覆盖权限与有效性约束。最终注入 Context 是一个受 Token Budget 约束的选择问题：</p><Formula label="Token Budget 下的 Memory 选择" className="compact-latex" tex={String.raw`\max_{x_i\in\{0,1\}}\sum_i x_iS_i\qquad\text{s.t.}\qquad\sum_i x_i\,\operatorname{tokens}(m_i)\le B_{\mathrm{memory}}`} /><aside className="text-note"><b>检索最相似，不等于检索最有用</b><p>“用户喜欢咖啡”与当前“取消订单”语义相近度可能不低，却没有任务价值；“该订单已退款”可能措辞差异很大，却是关键状态。Query 构造、Metadata Filter、Reranker 和实体键通常与 Embedding 同样重要。</p></aside></section>

      <section className="prose-section" id="chapter-9"><div className="prose-heading"><span>09</span><div><p>MEMORIES MUST EVOLVE</p><h2>新信息出现时，怎样更新、合并和遗忘？</h2></div></div><p>简单 Append-only 会让 Store 同时保存“用户住在上海”和“用户住在北京”，却不知道哪个仍有效。更新策略至少要区分新增、强化、修正、失效和删除：</p><div className="norm-table-wrap"><table className="norm-table"><thead><tr><th>操作</th><th>何时使用</th><th>例子</th></tr></thead><tbody><tr><td>Append</td><td>新的独立事件</td><td>本次订单已完成</td></tr><tr><td>Merge</td><td>多条证据支持同一事实</td><td>多次表达相同语言偏好</td></tr><tr><td>Supersede</td><td>新事实替代旧事实</td><td>通知方式从短信改为邮件</td></tr><tr><td>Invalidate</td><td>记录不再可靠但需留审计</td><td>过期地址、撤销授权</td></tr><tr><td>Delete</td><td>用户请求、法规或保留期到期</td><td>删除个人偏好与敏感信息</td></tr></tbody></table></div><Formula label="冲突记录的选择不应只看新旧" className="compact-latex" tex={String.raw`m^*=\arg\max_{m_i}\;\operatorname{authority}_i\cdot\operatorname{confidence}_i\cdot\operatorname{temporal\ validity}_i\cdot\operatorname{scope\ match}_i`} /><p>“更新”最好保留版本链：当前读路径只返回有效版本，审计仍能追溯旧值和变更来源。对低价值或长期未使用的记录，可以使用衰减作为候选信号：</p><Formula label="Recency Decay 只是遗忘信号之一" tex={String.raw`w_i(t)=w_i(t_i)\exp[-\lambda(t-t_i)]`} /><p>但不能机械地删除所有旧记录：身份证明、合同或用户明确长期偏好可能很久不被访问，却仍然重要。遗忘应同时考虑保留策略、法律义务、用户控制、重要性和最近验证时间。</p><aside className="answer-first"><span>Reflection 与 Consolidation</span><p>Generative Agents 将多条经历综合成更高层 Reflection；A-MEM 等方法进一步组织关联和演化。工程上应把这类生成内容标记为 Derived Memory，保留其来源记录，避免把模型总结当成原始事实。</p></aside></section>

      <section className="prose-section" id="chapter-10"><div className="prose-heading"><span>10</span><div><p>RETRIEVED DOES NOT MEAN TRUSTED</p><h2>Memory 被取出以后，怎样安全地放进 Context？</h2></div></div><p>检索结果必须以明确边界注入：标注 Type、Source、Timestamp、Confidence 和是否来自用户陈述；同时告诉模型它是参考证据，不是高于 System Instructions 的新指令。</p><Formula label="Context Token Budget 的分配" className="compact-latex" tex={String.raw`B_{\mathrm{ctx}}=B_{\mathrm{instructions}}+B_{\mathrm{state}}+B_{\mathrm{memory}}+B_{\mathrm{tools}}+B_{\mathrm{response}}`} /><div className="norm-table-wrap"><table className="norm-table"><thead><tr><th>注入策略</th><th>适合内容</th><th>风险</th></tr></thead><tbody><tr><td>直接结构化字段</td><td>稳定 Profile、实体 ID、权限状态</td><td>Schema 过时</td></tr><tr><td>Top-k 事实片段</td><td>离散 Semantic Facts</td><td>重复、冲突、语义误召回</td></tr><tr><td>Episode 摘要</td><td>过去案例与策略经验</td><td>案例迁移错误</td></tr><tr><td>Conversation Summary</td><td>长 Thread 压缩</td><td>摘要漂移和细节丢失</td></tr><tr><td>按需 Memory Tool</td><td>模型在执行中主动查询</td><td>多一步调用、可能漏查或滥查</td></tr></tbody></table></div><aside className="boundary-box"><b>Memory 也可能包含 Prompt Injection</b><p>外部网页、Tool Result 或历史消息中的恶意指令一旦被长期保存，会在未来反复污染决策。写入前要区分 Data 与 Instructions，读取后仍应按不可信内容处理，绝不能让 Retrieved Text 覆盖更高优先级规则。</p></aside></section>

      <section className="prose-section" id="chapter-11"><div className="prose-heading"><span>11</span><div><p>SHARING REQUIRES EXPLICIT OWNERSHIP</p><h2>多 Agent 应该怎样共享 Memory？</h2></div></div><p>多 Agent 系统不应默认共享全部历史。每条记录需要 Owner、Namespace、读写 ACL 和 Purpose。Manager 可以共享任务 State，而 Specialist 只接收完成子任务必要的最小片段。</p><Formula label="多 Agent Memory 访问控制" className="compact-latex" tex={String.raw`\operatorname{readable}(m_i,a_j)=\operatorname{scope}(m_i,a_j)\land\operatorname{ACL}(m_i,a_j)\land\operatorname{purpose}(m_i,a_j)`} /><div className="norm-table-wrap"><table className="norm-table"><thead><tr><th>信息</th><th>推荐 Scope</th><th>原因</th></tr></thead><tbody><tr><td>当前任务计划</td><td>Run / Team</td><td>协作所需，但不应跨任务泄露</td></tr><tr><td>用户偏好</td><td>User + Purpose</td><td>跨会话有用，但需授权和用途限制</td></tr><tr><td>Specialist 私有草稿</td><td>Agent / Subtask</td><td>避免无关上下文和敏感推理外泄</td></tr><tr><td>审核后的公共经验</td><td>Organization</td><td>允许多个 Agents 复用</td></tr><tr><td>原始凭证与 Secrets</td><td>Secret Manager，不进入 Memory</td><td>避免检索与 Prompt 暴露</td></tr></tbody></table></div><p>共享 Derived Memory 前还要解决责任归属：哪个 Agent 生成、依据哪些 Tool Results、是否经过审核、哪些版本能读。否则一条错误总结会在多个 Agents 之间传播并被重复强化。</p></section>

      <section className="prose-section" id="chapter-12"><div className="prose-heading"><span>12</span><div><p>MEMORY IS A DATA GOVERNANCE SYSTEM</p><h2>安全、隐私和用户控制应该怎样设计？</h2></div></div><div className="reason-list compact-reasons"><article><span>01</span><div><h3>Consent and purpose</h3><p>明确为什么记、用在哪里、是否跨会话与跨产品。</p></div></article><article><span>02</span><div><h3>Tenant isolation</h3><p>Namespace 和数据库查询都必须强制隔离，不能依赖 Prompt。</p></div></article><article><span>03</span><div><h3>Data minimization</h3><p>不保存完成目标不需要的敏感细节，Secrets 进入专用 Vault。</p></div></article><article><span>04</span><div><h3>User inspection</h3><p>用户能够查看、纠正、导出和删除长期记录。</p></div></article><article><span>05</span><div><h3>Retention and deletion</h3><p>定义过期时间、法律保留、软删除与物理删除语义。</p></div></article><article><span>06</span><div><h3>Provenance</h3><p>记录来源、写入者、模型版本、证据和每次修改。</p></div></article></div><aside className="answer-first"><span>个性化不等于无限保存</span><p><b>越多个人数据不一定带来越好体验。</b>不相关 Memory 增加检索噪声，过期 Memory 导致错误行动，敏感 Memory 扩大泄露面。最小化本身也是质量优化。</p></aside></section>

      <section className="prose-section" id="chapter-13"><div className="prose-heading"><span>13</span><div><p>FROM FRAMEWORK API TO DATA PIPELINE</p><h2>怎样用框架落地一个最小 Memory 系统，并验证它真的有用？</h2></div></div><p>先把框架能力和产品责任分开。框架可以提供 Checkpoint、Session、Store 接口和 Hook，但它不会替业务决定“用户这句话是否值得长期保存”“哪个系统才是权威来源”“删除请求应该影响哪些副本”。</p><div className="norm-table-wrap"><table className="norm-table"><thead><tr><th>实现入口</th><th>可直接获得</th><th>仍需自己建设</th></tr></thead><tbody><tr><td>LangGraph Checkpointer</td><td>Thread State 持久化、暂停与恢复</td><td>长期事实抽取、跨 Thread Namespace、治理</td></tr><tr><td>LangGraph Store</td><td>Namespace 下的记录读写与检索接口</td><td>Record Schema、Write Gate、版本冲突、ACL</td></tr><tr><td>LangChain Middleware</td><td>在模型前后做摘要、Context 注入和限制</td><td>业务真实性、长期 Store 与评测集</td></tr><tr><td>OpenAI Agents SDK Session</td><td>同一 Session 的消息历史自动读写</td><td>Semantic Profile、Episode、跨 Session Recall</td></tr><tr><td>自建 Memory Service</td><td>完全控制 Schema、数据库与 API</td><td>全部编排、Checkpoint 和可观测性</td></tr></tbody></table></div><div className="protocol-grid"><article className="protocol-card"><header><b>Read path · before model</b><span>SYNC / LOW LATENCY</span></header><pre className="protocol-code"><code>{"scope = resolve(tenant, user, thread)\nquery = build(goal, entities, state)\nrecords = store.search(scope, query, filters)\ncontext = rerank_dedup_budget(records)\nmodel_input += typed_memory(context)"}</code></pre></article><article className="protocol-card"><header><b>Write path · after observation</b><span>SYNC + ASYNC</span></header><pre className="protocol-code"><code>{"event_log.append(raw_message_or_tool_result)\ncandidates = extractor.propose(event)\nfor memory in policy.admit(candidates):\n    store.upsert_or_supersede(memory)\n    index.enqueue(memory.id)\naudit.record(decision, provenance)"}</code></pre></article></div><p>读路径通常在每次 Model Call 前同步执行，因此要控制 P95 延迟；写路径可以分层：关键 State 和用户明确偏好同步提交，摘要、Embedding、去重与 Reflection 交给异步 Worker。无论用什么框架，都应让 Memory 操作进入 Trace：候选来自哪里、为什么写入、检索过滤了什么、最终哪些记录进入了 Prompt。</p><p>Memory 评测不能只看向量检索 Recall。LongMemEval 将长期交互能力拆为信息抽取、跨会话推理、时间推理、知识更新和拒答；MemoryAgentBench 还强调准确检索、测试时学习、长程理解与选择性遗忘。</p><div className="norm-table-wrap"><table className="norm-table"><thead><tr><th>阶段</th><th>核心问题</th><th>指标示例</th></tr></thead><tbody><tr><td>Write</td><td>该记的是否记下，不该记的是否过滤？</td><td>Admission Precision / Recall、敏感写入率</td></tr><tr><td>Store</td><td>是否去重、带来源并正确版本化？</td><td>冲突率、重复率、Provenance 完整率</td></tr><tr><td>Retrieve</td><td>关键记录能否在预算内被取回？</td><td>Recall@k、NDCG、Token Cost、ACL 违规率</td></tr><tr><td>Use</td><td>取回内容是否改善最终决策？</td><td>Task Success、Grounded Accuracy、错误采用率</td></tr><tr><td>Update</td><td>新事实是否替换过期事实？</td><td>Knowledge Update Accuracy、Temporal Consistency</td></tr><tr><td>Forget</td><td>应删除的是否消失，重要信息是否保留？</td><td>Deletion SLA、Selective Forgetting、保留误删率</td></tr></tbody></table></div><aside className="answer-first"><span>推荐的起步顺序</span><p><b>先实现 Thread Checkpoint、结构化 User Profile、少量可溯源 Facts 和严格 Metadata Filter；用真实任务评测后，再增加 Episodes、Reflection、Graph Linking 或让 Agent 自主调用 Memory Tools。</b></p></aside></section>

      <section className="prose-section rms-qa" id="qa"><div className="prose-heading"><span>14</span><div><p>QUESTIONS AS CONNECTIONS</p><h2>关联 QA</h2></div></div><div className="qa-list">
        <details id="qa-1" open><summary><span>Q1</span>Memory 与 Context Window 有什么区别？</summary><div><p>Memory 是模型外的持久信息集合；Context 是本次调用实际送给模型的有限输入。检索的作用就是从 Memory 中选择少量记录进入 Context。</p></div></details>
        <details id="qa-2"><summary><span>Q2</span>保存完整 Chat History 不就够了吗？</summary><div><p>完整历史可以作为证据，但会持续增长、混入无关信息并保留过期事实。Memory 还需要抽取、合并、检索、更新和遗忘。</p></div></details>
        <details id="qa-3"><summary><span>Q3</span>Memory 与 RAG 有什么区别？</summary><div><p>RAG 常从外部知识库读取相对稳定的文档；Memory 更强调从交互中持续写入、个人或任务作用域、更新与生命周期治理。二者可共享检索技术。</p></div></details>
        <details id="qa-4"><summary><span>Q4</span>Vector Database 就等于 Memory 吗？</summary><div><p>不等于。它提供向量存储与相似度搜索，不负责 Write Gate、事实抽取、版本、冲突、权限、Context 注入和删除。</p></div></details>
        <details id="qa-5"><summary><span>Q5</span>KV Cache 是 Agent Memory 吗？</summary><div><p>不是。KV Cache 是当前序列 Attention 的计算缓存，通常随推理会话释放；Agent Memory 是可持久化、可检索、带作用域和治理语义的信息层。</p></div></details>
        <details id="qa-6"><summary><span>Q6</span>模型参数里的知识算 Memory 吗？</summary><div><p>在广义认知分类中可称为参数化或程序性知识，但工程上通常与可编辑的 Agent Memory 分开。参数难以按用户实时写入、精确删除和溯源。</p></div></details>
        <details id="qa-7"><summary><span>Q7</span>每条用户消息都应该写入吗？</summary><div><p>不应该。临时请求、闲聊、重复内容和敏感信息可能没有长期价值。应通过用户意图、规则或 Write Gate 决定是否写入。</p></div></details>
        <details id="qa-8"><summary><span>Q8</span>什么时候应该更新而不是新增？</summary><div><p>当新内容描述同一实体、同一字段或同一持续事实，并且意图是修正或替代时，应建立版本和 Supersede 关系；独立事件通常 Append。</p></div></details>
        <details id="qa-9"><summary><span>Q9</span>为什么检索最相似的内容仍可能出错？</summary><div><p>相似内容可能过期、无权限、来源不可靠或与当前任务无关。可靠检索还需要 Metadata Filter、时间、实体键、置信度、Reranker 和 Token Budget。</p></div></details>
        <details id="qa-10"><summary><span>Q10</span>怎样处理互相矛盾的记忆？</summary><div><p>不要把两条文本都无标记地注入。按实体和字段识别冲突，再依据权威来源、时间有效性、置信度和 Scope 选择当前版本，同时保留版本链。</p></div></details>
        <details id="qa-11"><summary><span>Q11</span>Agent 应该主动遗忘吗？</summary><div><p>应该有选择地遗忘或失效。目的包括减少噪声、遵守保留期和用户删除要求，但重要事实不能只因很久未访问就机械删除。</p></div></details>
        <details id="qa-12"><summary><span>Q12</span>多 Agent 能共享全部 Memory 吗？</summary><div><p>不应默认共享。按任务、用户、Agent 和组织建立 Namespace 与 ACL，只传递子任务必需的信息，并保留来源与责任归属。</p></div></details>
        <details id="qa-13"><summary><span>Q13</span>怎样评测 Memory 系统？</summary><div><p>分别评测写入、检索、使用、更新、时间推理、拒答和遗忘；还要测最终任务收益、Token 成本、延迟和权限违规，而不是只测 Vector Recall。</p></div></details>
        <details id="qa-14"><summary><span>Q14</span>Memory 会不会让 Agent 更容易产生幻觉？</summary><div><p>会。错误、过期或被注入污染的 Memory 会让模型更自信地采用错误信息。必须保存 Provenance、Confidence、Validity，并允许模型在证据不足或冲突时拒答和重新查询权威 Tool。</p></div></details>
        <details id="qa-15"><summary><span>Q15</span>框架里的 Session 就等于长期 Memory 吗？</summary><div><p>通常不等于。Session 主要帮助同一个会话自动保存和恢复消息历史，属于 Thread-scoped Continuity。长期 Memory 还需要从历史中抽取可复用事实或经历，放入 User/Agent/Organization Namespace，并提供跨 Session 检索、更新、权限和删除。</p></div></details>
        <details id="qa-16"><summary><span>Q16</span>LangGraph Checkpointer 与 Store 有什么区别？</summary><div><p>Checkpointer 保存某个 Thread 的 Graph State，使运行能够暂停、失败恢复和继续；Store 保存自定义 Namespace 下可跨 Threads 读取的记录。前者更像“这个任务执行到哪里”，后者更像“未来其他任务还能查到什么”。两者都可以落盘，但 Scope 不同。</p></div></details>
      </div></section>

      <section className="sources-section rms-sources"><div><p className="mini-label">PRIMARY SOURCES</p><h2>经典架构、近期评测与官方实现指南</h2></div><ol><li><a href="https://arxiv.org/abs/2304.03442" target="_blank" rel="noreferrer">Generative Agents · Memory Stream, Reflection and Planning</a><span>2023</span></li><li><a href="https://arxiv.org/abs/2310.08560" target="_blank" rel="noreferrer">MemGPT · Virtual Context Management</a><span>2023</span></li><li><a href="https://arxiv.org/abs/2410.10813" target="_blank" rel="noreferrer">LongMemEval · Long-Term Interactive Memory</a><span>2024</span></li><li><a href="https://arxiv.org/abs/2507.05257" target="_blank" rel="noreferrer">MemoryAgentBench · Incremental Multi-Turn Evaluation</a><span>2025</span></li><li><a href="https://docs.langchain.com/oss/python/concepts/memory" target="_blank" rel="noreferrer">LangGraph · Short-term Checkpoints and Long-term Stores</a><span>Official</span></li><li><a href="https://docs.langchain.com/oss/python/langchain/agents" target="_blank" rel="noreferrer">LangChain · Agent State and Middleware</a><span>Official</span></li><li><a href="https://openai.github.io/openai-agents-python/running_agents/" target="_blank" rel="noreferrer">OpenAI Agents SDK · Sessions and Conversation State</a><span>Official</span></li></ol></section>
      <footer className="article-footer"><div><span>继续连接知识</span><strong>Memory 不是 Agent 的附加聊天记录，而是决定长期状态如何写入、召回、演化和受控使用的系统层。</strong></div><div className="footer-links"><a href="/agents/agent">Agent 基础 ↗</a><a href="/agents/tools">Agent Tools ↗</a><a href="/foundations/importance-sampling">Importance Sampling ↗</a><a href="/training/reasoning-rl">Reasoning RL ↗</a></div></footer>
    </main>
    <aside className="right-rail" aria-label="本页目录"><p>本页目录</p>{chapters.map(([no, label]) => <a href={no === "14" ? "#qa" : `#chapter-${Number(no)}`} key={no}><span>{no}</span>{label}</a>)}<RightQaIndex questions={qaQuestions} /><div className="reading-note"><span>14 章</span><div><i></i></div><small>工程密度 · 高</small></div></aside>
  </div>;
}
